<%= link_to "Devise", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    Logged in as <strong><%= current_user.email %></strong>.
    <li>
    <%= link_to I18n.t('devise.shared.links.Logout'), destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to I18n.t('devise.shared.links.Login'), new_user_session_path %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to I18n.t('devise.shared.links.edit_profile'), edit_user_registration_path %>
    </li>
  <% else %>
    <li>
    <%= link_to I18n.t('devise.shared.links.sign_up'), new_user_registration_path %>
    </li>
  <% end %>
 </ul>

